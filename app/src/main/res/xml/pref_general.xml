<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="user_name"
        android:title="@string/pref_title_display_name"
        android:defaultValue="@string/pref_default_display_name"
        android:selectAllOnFocus="true"
        android:inputType="textCapWords"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="level_list"
        android:title="@string/pref_title_level_list"
        android:defaultValue="l1"
        android:entries="@array/pref_level_list_titles"
        android:entryValues="@array/pref_level_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="wordcount_per_day"
        android:title="@string/pref_title_wordcount_per_day"
        android:defaultValue="40"
        android:entries="@array/pref_wordcount_per_day_titles"
        android:entryValues="@array/pref_wordcount_per_day_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="flashcard_frequency"
        android:title="@string/pref_title_flashcard_frequency"
        android:entries="@array/pref_flashcard_frequency_titles"
        android:entryValues="@array/pref_flashcard_frequency_values"
        android:defaultValue="1000"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="testcard_frequency"
        android:title="@string/pref_title_testcard_frequency"
        android:entries="@array/pref_testcard_frequency_titles"
        android:entryValues="@array/pref_testcard_frequency_values"
        android:defaultValue="3000"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
